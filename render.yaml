services:
  - type: web
    name: api-images-ia
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_images:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
